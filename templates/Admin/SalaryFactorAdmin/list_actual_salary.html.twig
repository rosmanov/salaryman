{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% if object.getActualSalary() == 0.0 %}
        In Progress
    {% else %}
        {{ object.getActualSalary()|number_format_currency(object.getSalaryCurrencyCode()) }}
    {% endif %}
{% endblock %}
